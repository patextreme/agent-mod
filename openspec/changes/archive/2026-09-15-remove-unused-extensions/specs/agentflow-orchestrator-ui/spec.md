## REMOVED Requirements

### Requirement: Blocking full-screen orchestrator

**Reason**: The `agentflow-orchestrator-ui` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — AgentFlow's blocking full-screen Orchestrator, fleet view, and tap-in/steer/stop controls are gone.

### Requirement: Live agent overview

**Reason**: The `agentflow-orchestrator-ui` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — AgentFlow's blocking full-screen Orchestrator, fleet view, and tap-in/steer/stop controls are gone.

### Requirement: Streamed progress logs

**Reason**: The `agentflow-orchestrator-ui` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — AgentFlow's blocking full-screen Orchestrator, fleet view, and tap-in/steer/stop controls are gone.

### Requirement: Tap into a running agent

**Reason**: The `agentflow-orchestrator-ui` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — AgentFlow's blocking full-screen Orchestrator, fleet view, and tap-in/steer/stop controls are gone.

### Requirement: Steer a running agent

**Reason**: The `agentflow-orchestrator-ui` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — AgentFlow's blocking full-screen Orchestrator, fleet view, and tap-in/steer/stop controls are gone.

### Requirement: Stop a running agent

**Reason**: The `agentflow-orchestrator-ui` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — AgentFlow's blocking full-screen Orchestrator, fleet view, and tap-in/steer/stop controls are gone.

### Requirement: Non-TUI mode behavior

**Reason**: The `agentflow-orchestrator-ui` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — AgentFlow's blocking full-screen Orchestrator, fleet view, and tap-in/steer/stop controls are gone.
