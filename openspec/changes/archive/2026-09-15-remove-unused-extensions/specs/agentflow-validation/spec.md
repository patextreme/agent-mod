## REMOVED Requirements

### Requirement: On-demand flow script validation

**Reason**: The `agentflow-validation` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — the `agentflow_validate` tool and the `/af-validate` command are gone.

### Requirement: LLM-accessible validation tool

**Reason**: The `agentflow-validation` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — the `agentflow_validate` tool and the `/af-validate` command are gone.

### Requirement: Human-accessible validation command

**Reason**: The `agentflow-validation` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — the `agentflow_validate` tool and the `/af-validate` command are gone.

### Requirement: Graph-wide type-check diagnostics

**Reason**: The `agentflow-validation` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — the `agentflow_validate` tool and the `/af-validate` command are gone.

### Requirement: Conditional declaration injection

**Reason**: The `agentflow-validation` capability is retired because the AgentFlow extension is removed from this package; nothing replaces it.
**Migration**: None — the `agentflow_validate` tool and the `/af-validate` command are gone.
